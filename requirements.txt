# Core ML
numpy==1.24.4
pandas==2.0.3
scikit-learn==1.3.2
xgboost==2.0.3
gensim==4.3.2
nltk==3.8.1
joblib==1.3.2
tqdm==4.66.1
matplotlib==3.7.3
seaborn==0.13.2
scipy==1.11.4

# LLM (Ollama client)
ollama==0.2.1

# Utilities
psutil==5.9.6

# ── GPU / cuML (RAPIDS) ───────────────────────────────────────────────────────
# cuML is NOT pip-installable — install via conda or the RAPIDS installer:
#
#   Option A — conda (recommended):
#     conda install -c rapidsai -c conda-forge -c nvidia \
#         cuml=24.06 python=3.10 cuda-version=11.8
#
#   Option B — pip (RAPIDS 24.06, CUDA 11.8):
#     pip install \
#         --extra-index-url=https://pypi.nvidia.com \
#         cuml-cu11==24.6.*
#
#   Option C — on ICM cluster, load the module:
#     module load rapids/24.06
#
# cupy is installed automatically as a cuML dependency.
# If cuML is absent the pipeline falls back to scikit-learn automatically.
